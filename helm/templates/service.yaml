apiVersion: v1
kind: Service
metadata:
  name: {{ include "fatnotificationcat.fullname" . }}
  labels:
    {{- include "fatnotificationcat.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    {{- include "fatnotificationcat.labels" . | nindent 4 }}
