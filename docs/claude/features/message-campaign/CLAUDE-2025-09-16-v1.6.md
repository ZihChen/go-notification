# 實作DB資料搬遷系統 v1.6

## 完成狀態

⚠️ **狀態**: 設計規劃階段 (2025-09-16)  
🎯 **目標**: 大量歷史資料的高效搬遷與資料一致性保障  
📊 **資料規模**: 預估百萬等級資料量  
🔄 **實作模式**: UpdateOrCreate 模式確保資料同步的可靠性  

## 功能概述
要把過去三個月內的DB會員訊息資料搬遷到微服務的DB

## 技術實作

### 系統架構設計
1. **執行模式**: 使用 Go 指令集方式執行 Migrate 操作
2. **資料同步策略**: 採用 UpdateOrCreate 方式確保資料一致性
3. **重複執行安全**: 有主鍵約束，重複執行指令不會重複創建資料，而會更新特定欄位
4. **資料修復機制**: 如果有資料漏掉，補上欄位後重複執行指令可以把資料同步回去
5. **性能優化**: 考慮百萬量級以上的資料量，確保在同步資料上的效能

### 性能考量
- **批次處理**: 分批處理大量資料，避免記憶體溢出
- **並發控制**: 適當的並發處理提升效率
- **進度監控**: 提供遷移進度追蹤和日誌記錄
- **錯誤恢復**: 支援中斷重啟和失敗重試機制
### 資料搬遷流程

#### 1. message_campaign 資料搬遷
**來源**: `fatcat_staging.notifications` → **目標**: `ms_fatnotificationcat.message_campaign`
   - Migrate邏輯:
     - 透過created_at搜出前三個月的資料
     - category mapping 到 category
       - mapping表：
         - 1 -> member
         - 2 -> bonus
         - 3 -> others
       - item mapping 到 item
         - mapping表：
           - 1 -> registration
           - 2 -> identity_verification
           - 3 -> bank_card
           - 4 -> others
           - 5 -> event
           - 6 -> all
           - 7 -> mission
       - trigger_type 統一寫入 success
       - merchant_id
         - 到fatcat_staging.companies 找 id = 2的資料
         - 透過到fatcat_staging.companies 的 name 來搜尋 ms_fatnotificationcat.merchants 的 name
         - 將merchants.id 寫入 merchant_id
       - global_id
         - 使用uuid.NewString()產生uuid
       - title 同 title
       - content 同 content
       - status
         - deleted_at != nil 為 cancelled
         - deleted_at = nil 為 sent
       - target
         - focus mapping 到 target
           - mapping表：
             - 1 -> high_activity
             - 2 -> low_activity
             - 3 -> not_activity
             - 8 -> player
             - 9 -> level
             - 10 -> tag
             - 11 -> all
       - auto_send 同 auto_send
       - created_at mapping到 send_start_time、send_end_time、created_at
       - updated_at 同 updated_at
       - created_by 同 created_by
       - updated_by 同 updated_by
       - deleted_at 同 deleted_at
#### 2. player_message 資料搬遷
**來源**: `fatcat_staging.user_notifications` → **目標**: `ms_fatnotificationcat.player_message`
   - Migrate邏輯:
      - fatcat_staging.notifications.id 與 fatcat_staging.user_notifications.notification_id 關聯
      - ms_fatnotificationcat.message_campaign.id 與 ms_fatnotificationcat.player_message.campaign_id 關聯
      - 同步 message_campaign 的時候一並把關聯的 player_message 同步進 DB
        - global_player_id
          - fatcat_staging.companies 的 name + "PLAYER" + fatcat_staging.user_notifications.user_id組合成的字串
          - ex. 組起來像 FATCAT-PLAYER-16700386 中間用-區隔開來
          - 使用global_player_id 到 ms_fatnotificationcat.players 搜尋如果不存在就不用同步該筆資料到 player_message
      - player_id
        - 為 ms_fatnotificationcat.players 的 id
      - campaign_id
        - 為 ms_fatnotificationcat.message_campaign 的 id
      - is_read 同為 is_read
      - created_at 同 created_at
      - updated_at 同 updated_at

## 實作規劃

### Phase 1: 基礎架構建立
- [ ] 建立 Migrate Use Case 和 Repository 介面
- [ ] 實作資料映射邏輯和轉換函數
- [ ] 建立批次處理機制
- [ ] 設計錯誤處理和日誌系統

### Phase 2: 核心功能實作
- [ ] 實作 message_campaign 資料搬遷邏輯
- [ ] 實作 player_message 資料搬遷邏輯  
- [ ] 建立資料驗證機制
- [ ] 實作進度追蹤功能

### Phase 3: 性能優化與測試
- [ ] 批次大小調優
- [ ] 並發處理優化
- [ ] 記憶體使用優化
- [ ] 全面測試與驗證

### Phase 4: 生產部署準備
- [ ] 完整的回滾機制
- [ ] 監控和告警設定
- [ ] 操作手冊編寫
- [ ] 生產環境驗證

## 品質保證

### 資料一致性檢查
- 搬遷前後資料量對比
- 關鍵欄位值驗證
- 關聯關係完整性檢查
- 資料類型和格式驗證

### 性能基準
- 目標處理速度: 10,000+ 記錄/分鐘
- 記憶體使用限制: < 1GB
- 錯誤率控制: < 0.1%
- 支援中斷恢復: 100%

---

**文檔更新**: 2025-09-16  
**狀態**: 設計規劃階段  
**下一步**: 開始 Phase 1 基礎架構建立